[project]
name = "ai-casino"
version = "0.1.0"
description = "Agentic stock trading system with multi-agent architecture"
readme = "README.md"
requires-python = "==3.14.3"
dependencies = [
    # Technical Analysis
    "pandas==3.0.1",
    "pandas-ta-classic==0.4.47",
    "numpy==2.4.3",
    # Sentiment Analysis
    "transformers==5.3.0",
    "torch==2.11.0",
    # LLM & Agents
    "openai==2.30.0",
    "anthropic==0.86.0",
    "httpx==0.28.1",
    "pydantic-ai-slim==1.73.0",
    # Market Data
    "yfinance==1.2.0",
    "alpha-vantage==3.0.0",
    # Trading
    "alpaca-py==0.43.2",
    "backtesting==0.6.5",
    "bokeh>=3.9,<3.10",  # backtesting 0.6.5 incompatible with bokeh 3.8+
    # Optimization
    "optuna==4.8.0",
    "riskfolio-lib==7.2.1",
    "quantstats==0.0.81",
    # Database
    "sqlalchemy[asyncio]==2.0.48",
    "asyncpg==0.31.0",
    # Web Search
    "ddgs==9.11.4",
    # Reddit
    "praw==7.8.1",
    # Utilities
    "tenacity==9.1.4",
    "python-dotenv==1.2.2",
    "python-dateutil==2.9.0.post0",
    "rich==14.3.3",
    "loguru==0.7.3",
    "pydantic==2.12.5",
    "pydantic-settings==2.13.1",
    "pyyaml==6.0.3",
    "packaging>=24.0",  # Required by langsmith (transitive dependency)
    # API Server
    "fastapi==0.135.2",
    "uvicorn[standard]==0.42.0",
    # CLI & TUI
    "typer==0.24.1",
    "textual==8.2.0",
    "dependency-injector==4.49.0",  # DI container infrastructure
    "result==0.17.0",  # Go-style error handling
    "playwright>=1.58.0",
    "playwright-stealth>=2.0.2",
    "requests>=2.33.0",  # CVE-2026-25645: security fix
]

[project.optional-dependencies]
dev = [
    "pytest==9.0.2",
    "pytest-asyncio==1.3.0",
    "pytest-cov==7.1.0",
    "pytest-mock==3.15.1",
    "aiosqlite==0.22.1",
]
profiling = [
    "yappi>=1.6.0",
    "snakeviz>=2.2.0",
]

[project.scripts]
aicasino = "src.cli.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "slow: Slow tests",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[tool.pyrefly]
# Pyrefly uses strict type checking by default
# Configuration is minimal - strictness comes from complete type annotations + ANN rules
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
project-excludes = [
    "tests/**",
    "services/**",
    "src/agents/thesis_researcher.py",  # Decorator wrapper changes Awaitable->Coroutine signature
    "src/daemon/positions.py",  # Pyrefly module path resolution bug with BaseRepository[T] generics
    "src/v1/trades/brokers/alpaca.py",  # Alpaca SDK returns dict|Order union; pyrefly can't infer attrs
]

[tool.uv]
# Restrict dependency resolution to Unix platforms (Linux/macOS)
# Prevents Windows resolution failures in CI for platform-specific deps like riskfolio-lib
environments = ["sys_platform == 'linux' or sys_platform == 'darwin'"]
constraint-dependencies = [
    "tornado>=6.5.5",  # CVE-2026-31958, GHSA-78cv-mqj4-43f7
    "requests>=2.33.0",  # CVE-2026-25645
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
    "pyrefly==0.58.0",
    "ruff>=0.15.0",
]
