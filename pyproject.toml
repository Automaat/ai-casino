[project]
name = "ai-casino"
version = "0.1.0"
description = "Agentic stock trading system with multi-agent architecture"
readme = "README.md"
requires-python = "==3.14.7"
dependencies = [
    # Technical Analysis
    "pandas==3.0.5",
    "pandas-ta-classic==0.6.52",
    "numpy==2.5.2",
    # Sentiment Analysis
    "transformers==5.15.0",
    "torch==2.13.0",
    # LLM & Agents
    "openai==2.53.0",
    "anthropic==0.121.0",
    "httpx==0.28.1",
    "pydantic-ai-slim==2.27.1",
    # Market Data
    "yfinance==1.5.2",
    "alpha-vantage==3.0.0",
    # Trading
    "alpaca-py==0.43.5",
    "backtesting==0.6.6",
    "bokeh>=3.9,<3.10",  # backtesting 0.6.5 incompatible with bokeh 3.8+
    # Optimization
    "optuna==4.9.0",
    "riskfolio-lib==7.3.0",
    "quantstats==0.0.81",
    # Database
    "sqlalchemy[asyncio]==2.0.52",
    "asyncpg==0.31.0",
    # Web Search
    "ddgs==9.14.4",
    # Reddit
    "praw==8.0.2",
    # Utilities
    "tenacity==9.1.4",
    "python-dotenv==1.2.2",
    "python-dateutil==2.9.0.post0",
    "rich==15.0.0",
    "loguru==0.7.3",
    "pydantic==2.13.4",
    "pydantic-settings==2.15.0",  # GHSA-4xgf-cpjx-pc3j
    "pyyaml==6.0.3",
    "packaging>=24.0",  # Required by langsmith (transitive dependency)
    # API Server
    "fastapi==0.141.1",
    "uvicorn[standard]==0.52.1",
    # CLI & TUI
    "typer==0.27.1",
    "textual==8.2.8",
    "dependency-injector==4.49.1",  # DI container infrastructure
    "result==0.17.0",  # Go-style error handling
    "playwright>=1.58.0",
    "playwright-stealth>=2.0.2",
    "requests>=2.33.0",  # CVE-2026-25645: security fix
]

[project.optional-dependencies]
dev = [
    "pytest==9.1.1",
    "pytest-asyncio==1.4.0",
    "pytest-cov==7.1.0",
    "pytest-mock==3.15.1",
    "aiosqlite==0.22.1",
]
profiling = [
    "yappi>=1.6.0",
    "snakeviz>=2.2.0",
]

[project.scripts]
aicasino = "src.cli.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "slow: Slow tests",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[tool.pyrefly]
# Pyrefly uses strict type checking by default
# Configuration is minimal - strictness comes from complete type annotations + ANN rules
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
project-excludes = [
    "tests/**",
    "services/**",
    "src/agents/thesis_researcher.py",  # Decorator wrapper changes Awaitable->Coroutine signature
    "src/daemon/positions.py",  # Pyrefly module path resolution bug with BaseRepository[T] generics
    "src/v1/trades/brokers/alpaca.py",  # Alpaca SDK returns dict|Order union; pyrefly can't infer attrs
]

[tool.uv]
# Restrict dependency resolution to Unix platforms (Linux/macOS)
# Prevents Windows resolution failures in CI for platform-specific deps like riskfolio-lib
environments = ["sys_platform == 'linux' or sys_platform == 'darwin'"]
constraint-dependencies = [
    "tornado>=6.5.7",  # CVE-2026-31958, GHSA-78cv-mqj4-43f7, CVE-2026-49853/54/55, GHSA-pw6j-qg29-8w7f
    "requests>=2.33.0",  # CVE-2026-25645
    "lxml>=6.1.0",  # CVE-2026-41066
    "pillow>=12.2.0",  # CVE-2026-40192
    "aiohttp>=3.14.1",  # CVE-2026-34993/47265/50269/54273-54280
    "idna>=3.15",  # PYSEC-2026-215
    "msgpack>=1.2.1",  # GHSA-6v7p-g79w-8964
    "starlette>=1.3.1",  # PYSEC-2026-161, CVE-2026-48817/48818/54282/54283
    "urllib3>=2.7.0",  # PYSEC-2026-141, PYSEC-2026-142
    "pip>=26.1.2",  # PYSEC-2026-196
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
    "pyrefly==1.2.0",
    "ruff>=0.15.0",
]
